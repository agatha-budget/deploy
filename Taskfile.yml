version: '3'

tasks:
  default:
    cmds:
      - task --list-all
  hello:
    cmds:
      - echo "Bonjour à vous mes très chers amis !"
  update:
    cmds:
      - git pull
      - git -C ../config/ pull
      - sudo nixos-rebuild switch